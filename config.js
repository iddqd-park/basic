const MSX_CONFIG = {
    // MSX Screen Overlay Configuration
    // All values are in percentages (%) relative to the monitor image
    screen: {
        top: 29,      // Distance from top
        left: 20,     // Distance from left
        width: 60,    // Width of the screen
        height: 43,   // Height of the screen

        // Optional: Fine-tuning for rotation or perspective if needed later
        // rotate: 0, 
    }
};
